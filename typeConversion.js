//converting datatypes

//e.g 
let age = window.prompt('how old are you')
age = Number(age);
age +=1;
//because of string concatenation it transform into string instead of number.
//so reassign age = number.
console.log(age)